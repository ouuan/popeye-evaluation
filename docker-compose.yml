services:
  pardiff:
    build: ParDiff
    volumes:
      - ./output/pardiff:/output
      - ./tests:/tests
      - ./third_party:/third_party
  popeye:
    build: netlifter
    volumes:
      - ./output/popeye:/output
      - ./tests:/tests
      - ./third_party:/third_party
